---
title: "Reference"
description: "Developer and power-user reference material for OpenLoomi — agent runtimes, the openloomi-ctl CLI, and embedding providers"
icon: "FaBook"
---

# Reference

Developer and power-user reference material for OpenLoomi. The pages here document the contracts that don't fit neatly into a user-guide chapter — runtime configuration, embedding providers, and the OpenLoomi CLI.

## Choose your path

| If you are…                                         | Start with…                                                                                                   |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| Picking a runtime to execute agent tasks            | [Agent Runtimes](/docs/reference/agent-runtimes) — Claude, Codex, OpenCode, Hermes, OpenClaw                  |
| Driving OpenLoomi from a shell or CI                | [OpenLoomi CLI](/docs/reference/openloomi-ctl) — `openloomi-ctl` from packaged desktop installs               |
| Switching between cloud and local embeddings        | [Embedding Providers](/docs/reference/embedding-providers) — providers, switching, and re-embedding behaviour |
| New to OpenLoomi and looking for user-guide content | [Welcome](/docs) — back to the top-level docs                                                                 |

## Overview

The reference section is grouped into two buckets:

- **Runtimes & APIs** — the [Agent Runtimes](/docs/reference/agent-runtimes) you can plug into OpenLoomi, plus the [OpenLoomi CLI](/docs/reference/openloomi-ctl) for one-shot automation from a shell or CI.
- **Configuration** — [Embedding Providers](/docs/reference/embedding-providers) for cloud-vs-local search backends.

If you're looking for something end-user-facing, head back to the [Welcome](/docs) page instead — most user-facing surfaces have their own page there.

## Available references

- **[Agent Runtimes](/docs/reference/agent-runtimes)** — Configure the default Claude runtime, the secondary Codex option, and other CLI runtimes for native agent tasks.
- **[OpenLoomi CLI](/docs/reference/openloomi-ctl)** — Run `openloomi-ctl` from packaged desktop installs for one-shot automation and CI/CD workflows.
- **[Embedding Providers](/docs/reference/embedding-providers)** — Cloud vs local embedding backends, switching, and re-embedding behaviour.

## Related

- [Plugins](/docs/plugins) — bridge to Claude Code and Codex CLI
- [Memory](/docs/memory) — where local data and embedding vectors live
- [Getting Started](/docs/getting-started) — install + first run
- [Glossary](/docs/glossary) — every term used across these reference pages
